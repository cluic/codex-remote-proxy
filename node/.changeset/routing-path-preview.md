---
"@cluic/codex-remote-proxy": patch
---

Add a compact live Overview route preview that traces account preference, matched routing rules, Provider order, model rewrites, predicted outlets, and conditional fallbacks for an exact model, with advanced retry details available on demand and a configuration-only fallback while the Worker is stopped.
