---
"@cluic/codex-remote-proxy": patch
---

Add a live Overview route preview that traces account preference, matched routing rules, Provider order, model rewrites, predicted outlets, and conditional fallbacks for an exact model, with a configuration-only fallback while the Worker is stopped.
