---
"@cluic/codex-remote-proxy": patch
---

Show compact path/query text in Forwarding Records and hide `/models` catalog traffic by default behind a pagination-safe, summary-aware visibility toggle while preserving inclusive API behavior for callers that omit the new option.
